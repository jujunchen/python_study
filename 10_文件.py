# file = open("test.txt", "w")
# file.write("hello world")
# file.close()

# read(num) 读取指定字节数，不写参数则读取全部
# readline() 读取一行 
# readlines() 读取所有行，返回列表

# seek(偏移量，起始位置) 移动文件指针到指定位置，0 开头，1 当前位置，2 结尾

# 
import os
os.mkdir("test")